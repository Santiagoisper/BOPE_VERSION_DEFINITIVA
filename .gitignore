node_modules/
dist/
.turbo/
.cache/
.local/
coverage/
*.tsbuildinfo
.env
.env.*
!.env.example
Thumbs.db
.DS_Store

.vercel
.operator-temp
.env.operator

# Claude Code local config — no versionar
.claude/settings.local.json

# Worktrees enlazados (mismo repo): no versionar como gitlink; evita drift infinito
.claude/worktrees/

# Logs de debug — no versionar
debug.log
*.log.debug

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Archivos temporales de agentes / diagnóstico local
_agent_*.txt

# MCP / npm accidental en raíz (monorepo pnpm en workspace superior)
/package-lock.json
mcp-debug.log

# Cursor hooks: buffer efímero; el índice incremental sí se versiona
.cursor/hooks/state/continual-learning.json
